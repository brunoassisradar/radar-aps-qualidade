import { Meta } from '@storybook/blocks';

<Meta title="Introdução" />

# Radar APS - Design System

Bem-vindo à biblioteca de componentes do **Radar APS**, construída seguindo a metodologia **Atomic Design**.

## 🎯 O que é Atomic Design?

Atomic Design é uma metodologia criada por Brad Frost que organiza componentes em 5 níveis hierárquicos:

| Nível | Descrição | Exemplos |
|-------|-----------|----------|
| **Tokens** | Valores de design fundamentais | Cores, tipografia, espaçamentos |
| **Atoms** | Elementos básicos indivisíveis | Button, Input, Badge, StatusBadge |
| **Molecules** | Grupos de átomos funcionando juntos | FilterBar, PageHeader, InfoCard, FAQAccordion |
| **Organisms** | Seções complexas de UI | AppSidebar, OverviewTable, IndicatorChart |
| **Templates** | Layouts de página completos | AppLayout, DashboardTemplate, RelatorioTemplate |

---

## 📦 Como Usar a Biblioteca

### Importação por Nível (Recomendado)

```tsx
// Átomos - Componentes básicos
import { Button, Input, Badge, StatusBadge, Skeleton } from '@/components/atoms';

// Moléculas - Componentes compostos
import { InfoCard, FilterBar, PageHeader, FAQAccordion, PeriodSelector } from '@/components/molecules';

// Organismos - Seções complexas
import { AppHeader, AppSidebar, OverviewTable, IndicatorChart, ReportTable } from '@/components/organisms';

// Templates - Layouts de página
import { AppLayout, DashboardTemplate, VisaoGeralTemplate, RelatorioTemplate } from '@/components/templates';
```

### Importação Unificada

```tsx
// Tudo em um só import
import { 
  Button, 
  InfoCard, 
  AppHeader, 
  AppLayout 
} from '@/components';
```

### Design Tokens

```tsx
import { colors, typography, spacing, shadows } from '@/lib/design-tokens';

// Exemplo de uso
console.log(colors.status.otimo.hex);     // "#3C8DBC"
console.log(typography.fontSize.base);     // "1rem"
console.log(spacing.scale[4]);             // "1rem"
```

---

## 🎨 Design Tokens

O sistema utiliza CSS Variables (HSL) para máxima flexibilidade:

### Cores Principais
| Token | Valor | Uso |
|-------|-------|-----|
| `--primary` | Azul (#0064FF) | Ações principais, links |
| `--background` | Branco/Escuro | Fundo da aplicação |
| `--foreground` | Preto/Branco | Texto principal |
| `--muted` | Cinza | Elementos secundários |
| `--accent` | Azul claro | Destaques sutis |

### Status de Indicadores
| Status | Cor | Significado |
|--------|-----|-------------|
| `--status-otimo` | Azul (#3C8DBC) | Meta alcançada com excelência |
| `--status-bom` | Verde (#00A65A) | Meta alcançada |
| `--status-suficiente` | Amarelo (#F0AD4E) | Próximo da meta |
| `--status-regular` | Vermelho (#DD4B39) | Abaixo da meta |

---

## 🧱 Componentes Disponíveis

### Atoms (7 componentes)
- **Button** - Botão com 6 variantes (default, destructive, outline, secondary, ghost, link)
- **Input** - Campo de entrada de texto
- **Label** - Rótulo de formulário com variantes
- **Badge** - Badge/tag pequena com variantes de cor
- **StatusBadge** - Badge de status com cores semânticas (ótimo, bom, suficiente, regular)
- **Skeleton** - Placeholder de carregamento animado
- **Separator** - Linha divisória horizontal/vertical

### Molecules (6 componentes)
- **ClassificationCard** - Card de classificação com ícone e contador
- **InfoCard** - Card informativo com links de navegação
- **PeriodSelector** - Seletor de período com tabs
- **FilterBar** - Barra de filtros com selects e progresso
- **PageHeader** - Cabeçalho de página com breadcrumb e ações
- **FAQAccordion** - Acordeão de perguntas frequentes

### Organisms (6 componentes)
- **AppHeader** - Cabeçalho da aplicação com logo e ações
- **AppSidebar** - Menu lateral hierárquico
- **OverviewTable** - Tabela de visão geral com expansão
- **ReportTable** - Tabela de relatórios com status
- **ResultadoMunicipio** - Card de resultado do município
- **IndicatorChart** - Gráfico de indicadores empilhado

### Templates (4 componentes)
- **AppLayout** - Layout principal com sidebar colapsável
- **DashboardTemplate** - Template de dashboard com cards
- **VisaoGeralTemplate** - Template com tabs e filtros
- **RelatorioTemplate** - Template de relatório com segmentos

---

## 🛠 Tecnologias

Esta biblioteca é construída com:

| Tecnologia | Versão | Propósito |
|------------|--------|-----------|
| React | 18.x | Framework UI |
| TypeScript | 5.x | Tipagem estática |
| Tailwind CSS | 3.x | Estilização utility-first |
| Ant Design | 4.x | Componentes base complexos |
| Radix UI | Latest | Primitivas acessíveis |
| CVA | 0.7.x | Class Variance Authority |
| Storybook | 8.x | Documentação e desenvolvimento |

---

## 🚀 Estrutura de Pastas

```
src/
├── lib/
│   └── design-tokens/          # Tokens de design
│       ├── colors.ts           # Paleta de cores
│       ├── typography.ts       # Tipografia
│       ├── spacing.ts          # Espaçamentos
│       ├── shadows.ts          # Sombras e bordas
│       └── index.ts            # Export unificado
│
├── components/
│   ├── atoms/                  # Componentes básicos
│   │   ├── Button/
│   │   ├── Input/
│   │   ├── Badge/
│   │   ├── StatusBadge/
│   │   └── index.ts
│   │
│   ├── molecules/              # Combinações de átomos
│   │   ├── InfoCard/
│   │   ├── FilterBar/
│   │   ├── PageHeader/
│   │   └── index.ts
│   │
│   ├── organisms/              # Seções complexas
│   │   ├── AppHeader/
│   │   ├── AppSidebar/
│   │   ├── OverviewTable/
│   │   └── index.ts
│   │
│   ├── templates/              # Layouts de página
│   │   ├── AppLayout/
│   │   ├── DashboardTemplate/
│   │   └── index.ts
│   │
│   └── index.ts                # Export unificado
│
└── stories/
    └── DesignTokens/           # Documentação visual
        ├── Colors.mdx
        ├── Typography.mdx
        ├── Spacing.mdx
        └── Shadows.mdx
```

---

## 📚 Navegando pelo Storybook

Use a sidebar à esquerda para explorar:

1. **Design Tokens** - Documentação visual de cores, tipografia e espaçamentos
2. **Atoms** - Componentes fundamentais com todas as variantes
3. **Molecules** - Componentes compostos reutilizáveis
4. **Organisms** - Seções completas de UI
5. **Templates** - Layouts de página prontos para uso

Cada componente possui:
- **Docs** - Documentação automática com props
- **Controls** - Playground interativo
- **Stories** - Exemplos de uso real

---

## 💡 Exemplos de Uso

### Criando uma Página de Dashboard

```tsx
import { AppLayout, DashboardTemplate } from '@/components/templates';
import { Book, FileText, HelpCircle } from 'lucide-react';

const cards = [
  {
    icon: Book,
    title: 'Documentação',
    description: 'Acesse a documentação completa',
    links: [{ label: 'Ver docs', href: '/docs' }],
  },
  {
    icon: FileText,
    title: 'Relatórios',
    description: 'Visualize seus relatórios',
    links: [{ label: 'Abrir', href: '/reports' }],
  },
];

const faqItems = [
  { question: 'Como usar?', answer: 'É simples!' },
];

export default function MyDashboard() {
  return (
    <AppLayout>
      <DashboardTemplate
        breadcrumbItems={[{ label: 'Home' }]}
        title="Meu Dashboard"
        cards={cards}
        faqItems={faqItems}
      />
    </AppLayout>
  );
}
```

### Criando uma Tabela de Visão Geral

```tsx
import { VisaoGeralTemplate } from '@/components/templates';
import { OverviewTable } from '@/components/organisms';

const tabs = [
  {
    key: 'qualidade',
    label: 'Qualidade',
    content: <OverviewTable />,
  },
  {
    key: 'vinculo',
    label: 'Vínculo',
    content: <div>Conteúdo de vínculo</div>,
  },
];

export default function VisaoGeral() {
  return (
    <VisaoGeralTemplate
      breadcrumbItems={[{ label: 'Início', href: '/' }, { label: 'Visão Geral' }]}
      title="Visão Geral"
      tabs={tabs}
      defaultActiveTab="qualidade"
    />
  );
}
```

---

Desenvolvido para o sistema de monitoramento da **Atenção Primária à Saúde**.
